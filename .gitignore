# Raw SSH session dumps may contain secrets
cli_dump/

.env
.venv/
__pycache__/
*.pyc
.DS_Store
/nse-status
/nse-app
*.exe
/cmd/nse-status/nse-status
/cmd/nse-app/nse-app
/dist/
/build/
dist.zip
profiles.json
prefs.json
known_hosts.json
known_hosts

# Superseded Python prototype, kept on disk but not part of this repo
gui/
tests/
.pytest_cache/

# Runtime state written next to the settings file. history.json and
# journal.json hold a site's measurements and its (redacted) running
# configuration; neither belongs in the repository.
history.json
journal.json
history.json.tmp
journal.json.tmp

# Design-tooling state, local to whoever is working on the UI.
.impeccable/
